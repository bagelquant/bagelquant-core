# boxcox

```python
boxcox(source, *, lambda_=0, name=None, metadata=None)
```

Apply the Box-Cox power transform element-wise with the supplied lambda.

## Parameters

**source** : Panel | Graph
: Input numeric `Panel` or single-output `Graph`.
**lambda_** : float, default `0`
: Box-Cox lambda parameter. Use `0` for the logarithmic limit.
**name** : str | None, default `None`
: Optional graph-node name. A generated name is used when omitted.
**metadata** : Mapping[str, Any] | None, default `None`
: Optional metadata stored on the graph node.

## Returns

**Graph**
: Lazy single-output graph. Call `.compute()` to materialize a `Panel`.

## Examples

```python
boxcox(source)
```

The tables below are generated by executing this operation against the shown
Panel fixtures.

### source

| time | asset_id | value |
|---|---|---:|
| 2024-01-02 | a | 1.0 |
| 2024-01-02 | b | 4.0 |
| 2024-01-02 | c | 3.0 |
| 2024-01-03 | a | missing |
| 2024-01-03 | b | 2.0 |
| 2024-01-03 | c | 8.0 |

### Output

| time | asset_id | value |
|---|---|---:|
| 2024-01-02 | a | 0.0 |
| 2024-01-02 | b | 1.3862943611198906 |
| 2024-01-02 | c | 1.0986122886681098 |
| 2024-01-03 | a | missing |
| 2024-01-03 | b | 0.6931471805599453 |
| 2024-01-03 | c | 2.0794415416798357 |

## Notes

Inputs are long-form panels keyed by `(time, asset_id)`.
